# SPDX-FileCopyrightText: 2025 Digg - Agency for Digital Government
#
# SPDX-License-Identifier: CC0-1.0

name: Pull Request Workflow

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pr-checks:
    uses: diggsweden/reusable-ci/.github/workflows/pullrequest-orchestrator.yml@a51c0edbdb0799550d4293863edfc4d8f1643742 # test feat/build-sbom-enhanced
    permissions:
      contents: read
      packages: read
      security-events: write
    secrets: inherit
    with:
      reusable-ci-ref: a51c0edbdb0799550d4293863edfc4d8f1643742
      project-type: gradle
      # Use devbase-check linting (replaces megalint, commitlint, licenselint)
      linters.devbasecheck: true
      linters.megalint: false
      linters.commitlint: false
      linters.licenselint: false
      linters.publiccodelint: false

  test:
    needs: [pr-checks]
    if: always()
    secrets: inherit
    permissions:
      contents: read
      packages: read
    uses: ./.github/workflows/test.yml
