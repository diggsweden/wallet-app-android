# SPDX-FileCopyrightText: 2025 Digg - Agency for Digital Government
#
# SPDX-License-Identifier: CC0-1.0

name: Release Dev Workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # Build Android app with signing
  build:
    uses: diggsweden/reusable-ci/.github/workflows/build-gradle-app.yml@d31c989acd139c3e8754e116496c826af8dbbca0 # v2.8.2
    permissions:
      contents: read
    secrets: inherit
    with:
      reusable-ci-ref: d31c989acd139c3e8754e116496c826af8dbbca0
      java-version: "21"
      build-module: "app"
      gradle-tasks: "assembleDemoRelease bundleDemoRelease"
      enable-signing: true
      artifact-name: "wallet-android-demo"

  # Publish to Google Play internal track
  publish-google-play:
    needs: build
    uses: diggsweden/reusable-ci/.github/workflows/publish-google-play.yml@d31c989acd139c3e8754e116496c826af8dbbca0 # v2.8.2
    permissions:
      contents: read
    secrets: inherit
    with:
      reusable-ci-ref: d31c989acd139c3e8754e116496c826af8dbbca0
      aab-artifact-name: "wallet-android-demo"
      package-name: "se.digg.wallet.demo"
      track: "internal"
      status: "completed"
